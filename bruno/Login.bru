meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: none
}

body:json {
  {
    "user_id": "testuser",
    "password": "mypassword"
  }
}

vars:pre-request {
  baseUrl: http://localhost:9991
}
